{
  "name": "blackbird-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf .test-dist && tsc --target ES2022 --module ES2022 --moduleResolution bundler --outDir .test-dist src/lib/filter.ts src/lib/sort.ts && node test/filter.test.mjs && rm -rf .test-dist"
  },
  "dependencies": {
    "solid-js": "^1.9.5"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.2.0",
    "vite-plugin-solid": "^2.11.6"
  }
}
